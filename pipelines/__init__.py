"""End-to-end workflow execution scripts (`pipelines/`).

Pipelines combine `apps` connectors and `automation` utilities into
direct, readable Python workflows that can be run from the CLI or scheduled.
"""
